---
title: TanStack Start
description: "@better-translate/tanstack-start is a scaffold package for TanStack Start integration."
---

# TanStack Start

`@better-translate/tanstack-start` is
a scaffold package for TanStack Start integration.

> **Work in progress** — This adapter is not fully implemented yet. The package exists as a scaffold.
> TanStack Start integration is on the roadmap. For now, you can use the core
> package directly with TanStack Start using the global helpers.

<Accordion type="single" collapsible className="mt-6">
  <AccordionItem value="today">
    <AccordionTrigger>What works today</AccordionTrigger>
    <AccordionContent>
      <p>Use `better-translate` for the locale engine and `@better-translate/react` when you need client-side switching.</p>
    </AccordionContent>
  </AccordionItem>
  <AccordionItem value="roadmap">
    <AccordionTrigger>What is planned</AccordionTrigger>
    <AccordionContent>
      <p>A first-party TanStack Start adapter is still on the roadmap, but the scaffold package already reserves the package slot.</p>
    </AccordionContent>
  </AccordionItem>
</Accordion>

## Using core with TanStack Start today

Until the dedicated adapter ships, you can use the core package and React adapter
directly in TanStack Start apps. Configure translations at the app entry point and
use global helpers or the React provider in your components.

## Package location

`packages/tanstack-start`

<div className="mt-6 flex flex-wrap gap-3">
  <Button asChild variant="outline" size="lg">
    <Link href="/docs/adapters/core">Use the core package today</Link>
  </Button>
  <Button asChild size="lg">
    <Link href="/docs/adapters/react">Pair it with React</Link>
  </Button>
</div>
