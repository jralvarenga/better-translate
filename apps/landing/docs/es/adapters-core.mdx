---
title: Core
description: "@better-translate/core funciona en cualquier proyecto TypeScript. Misma configuración, misma API, sin importar el framework o el runtime."
---

# Core

`@better-translate/core` funciona en cualquier proyecto TypeScript. Sin dependencia de framework ni requisito de runtime concreto. La misma configuración y la misma API aplican en Next.js, Astro, React, Bun, Node.js, un script o cualquier otro entorno TypeScript.

Los **adaptadores** son extensiones de core que añaden utilidades específicas del framework: enrutamiento con locale, contexto de React, helpers por petición, etc. Pero todo se construye sobre el mismo core.

## Cuándo usar core directamente

Usa core sin adaptador cuando no necesites utilidades del framework. Incluye servidores, APIs, scripts, bibliotecas compartidas o cualquier entorno TypeScript donde no exista adaptador para tu stack.

## 1. Instalar el paquete

```sh
npm install @better-translate/core
```

## 2. Crear un archivo de traductor

Crea `src/i18n.ts`:

```ts
import { configureTranslations } from "@better-translate/core";

const en = {
  home: {
    title: "Hello",
    description: "This is the English version.",
  },
} as const;

const es = {
  home: {
    title: "Hola",
    description: "Esta es la version en espanol.",
  },
} as const;

export const translator = await configureTranslations({
  availableLocales: ["en", "es"] as const,
  defaultLocale: "en",
  fallbackLocale: "en",
  messages: { en, es },
});
```

## 3. Usar el traductor en cualquier sitio

```ts
import { translator } from "./i18n";

translator.t("home.title"); // "Hello"
translator.t("home.title", { locale: "es" }); // "Hola"
translator.t("home.description", { locale: "es" });
```

## 4. Añadir otro locale más tarde

```ts
const fr = {
  home: {
    title: "Bonjour",
    description: "Ceci est la version francaise.",
  },
} as const;

export const translator = await configureTranslations({
  availableLocales: ["en", "es", "fr"] as const,
  defaultLocale: "en",
  fallbackLocale: "en",
  messages: { en, es, fr },
});
```

## Comportamiento de fallback

Si el locale activo no tiene una clave, better-translate prueba el locale de fallback.
Si el fallback tampoco la tiene, devuelve la cadena de la clave.

- → Falta el valor en el locale → valor del locale de fallback
- → Falta también en el fallback → cadena de la clave

## Loaders asíncronos

Registra loaders de locale para idiomas que no quieras precargar. Los locales cargados se cachean tras la primera carga correcta.

```ts
const translator = await configureTranslations({
  availableLocales: ["en", "fr"] as const,
  defaultLocale: "en",
  messages: { en },
  loaders: {
    fr: async () => import("./messages/fr").then((m) => m.default),
  },
});

// carga fr bajo demanda y lo cachea
await translator.loadLocale("fr");
```

## 5. Extraer cadenas automáticamente con la CLI

En lugar de nombrar claves a mano, marca cadenas con `{ bt: true }` y deja que la CLI las extraiga y les asigne clave:

```ts
import { t } from "@better-translate/core";

// Escribe la cadena fuente directamente — la CLI la convierte en clave correcta
t("Hello world", { bt: true });
```

Ejecuta `npx bt extract` para sincronizar las cadenas en tu archivo de locale fuente y reescribir las llamadas a claves correctas. Consulta la [documentación de la CLI](/docs/cli) para la configuración completa.

## Continuar con

- <Link href="/docs/adapters/react">Provider y hooks de React</Link>
- <Link href="/docs/adapters/nextjs">Configuración de Next.js App Router</Link>
- <Link href="/docs/adapters/astro">Helpers de petición en Astro</Link>

## Ejemplos

- [core-elysia-example](https://github.com/jralvarenga/better-translate/tree/main/examples/core-elysia-example) — configuración TypeScript/Node.js plana
